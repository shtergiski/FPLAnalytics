# FPL API Proxy — Cloudflare Worker URL
# After deploying: cd worker && npx wrangler deploy
# Then paste the worker URL here (no trailing slash)
# Example: VITE_FPL_WORKER_URL=https://fpl-api-proxy.your-account.workers.dev
VITE_FPL_WORKER_URL=
